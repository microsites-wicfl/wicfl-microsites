# Temporary, disposable config used only by .github/workflows/smoke-test-deploy.yml
# to confirm CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID authenticate correctly.
#
# This is NOT the production wrangler.toml. The real config (Worker names, pods of
# ~25 sites, production routes) is created under W-014 once domains are approved
# (W-007/W-011). Delete this file once the smoke test has been verified once.

name = "wicfl-microsites-smoke-test"
compatibility_date = "2026-09-03"

[assets]
directory = "./dist/sites/_example"
