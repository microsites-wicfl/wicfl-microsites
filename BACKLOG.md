# BACKLOG — WICFL Microsites

Items priorizados. **Solo cowork crea items nuevos.** El agente ejecutor puede cerrar el
item que su prompt cerraba, con refs a prompt/report/commit, pero no crea ni reprioriza.

Formato de id: `W-NNN` · Última actualización: 2026-09-21 (W-116/W-117 cerrados, W-118 avanza — Aprobado con hallazgos por cowork, pendiente de credenciales; W-102 avanza con el guión de video para Pavel — ver `BITACORA.md` 2026-09-21)

---

## Bloqueadores

| id | Item | Dueño | Debido | Bloquea |
|---|---|---|---|---|
| W-008 | **Assets de marca de Walker**: logo, contacto aprobado, disclosures requeridos, license numbers | Kevin | 2026-09-04 | Sitio #1 no puede publicar sin esto. *(aporte de Pavel)* **Pregunta de Pavel el 2026-09-04** en el chat del equipo, viendo el demo: "How can we add the logo and the graphic elements?" Reveló que hasta hoy no existía ni el campo en el schema ni el soporte en el template — el wordmark del header era puro texto, `site.brand.name`, sin ningún gancho para imagen. Se cerró la parte técnica hoy mismo, ver **W-109**. Lo que sigue faltando y es 100% de este ítem: que Kevin entregue el archivo real del logo. **Avance 2026-09-08:** Vic confirma que Kevin ya dio luz verde y que Pavel ya tiene listo el contenido de marca: logo, contenido, etc. **No se cierra todavía** — falta recibir los archivos/datos reales (logo, license number, dirección y email aprobados) para verificarlos contra `docs/SITE_CONTENT_CHECKLIST.md` y escribirlos en `sites/stuart-homeowners/site.config.json` + `sites/stuart-homeowners/public/`. Se cierra cuando esos campos dejen de ser placeholder en el config real, no antes **Avance 2026-09-09:** Pavel le pasó a Vic el archivo del logo (`logo stuart.svg`), ya descargado en su equipo. Con esto la parte del logo ya no depende de Kevin. Sigue faltando de Kevin: el license number, y el teléfono, email y dirección aprobados para el sitio. Vic ya le mandó el mensaje a Kevin pidiendo esos datos, por el chat de Zoom, hoy 2026-09-09. **Avance 2026-09-09 (2):** el logo quedó instalado y mergeado a `main` vía PR #7 (`brand.logo` en `site.config.json` + `sites/stuart-homeowners/public/logo.svg`), verificado en el preview deploy antes de mergear. Ver `BITACORA.md` 2026-09-09. Con esto la parte del logo queda resuelta del todo; sigue abierto el resto: license number, teléfono, email y dirección aprobados, ya pedidos a Kevin. **Avance 2026-09-22:** Kevin dice que **no hace falta mostrar el número de licencia** en los sitios. Verificado contra la guía de cumplimiento del Florida DFS (myfloridacfo.com, General Guidelines): el número de licencia se exige en las solicitudes de póliza, no en publicidad. Consecuencia: el bloqueador se reduce a teléfono, email y dirección aprobados. Falta un prompt chico para volver `contact.licenseNumber` opcional en el schema (cambio compatible, no rompe configs), dejar de renderizarlo en el template y sacarlo del gate de W-103; hasta entonces el gate sigue bloqueando por el placeholder de licencia. **Prompt listo:** `prompts/2026-09-22_016_licencia-opcional.md` (campo opcional en schema, render condicional, fuera del config de Stuart y del formulario, docs). **Avance 2026-09-22 (prompt 016, commits `0a8fd39`/`d7c12ac`, CI run 35766505678):** licencia fuera. Schema la acepta opcional, el footer la muestra solo si existe, Stuart y el formulario ya no la escriben. **Este ítem queda en: teléfono, email y dirección aprobados de Kevin** para Stuart. |

---

## Fase 0 — Decisiones de Kevin (24–28 ago 2026)

**Priorizado el 2026-08-28.** No se le manda a Kevin la lista completa: se satura y no
contesta ninguna. Solo **W-092 y W-093** salen ahora, porque son las dos únicas que bloquean
la creación de cuentas. El resto se empuja después, en orden: W-006 (afecta la validación de
nichos que Pavel arranca esta semana), W-008 y W-095 (bloquean publicar, no construir), y
W-005 con W-100 (se piden juntas, con la propuesta ya escrita).

| id | Item | Dueño | Debido | Bloquea |
|---|---|---|---|---|
| W-003 | Nicho y mercado en inglés para sitio #1 | **Pavel propone**, Kevin aprueba | 2026-08-28 | W-016. **Reasignado el 26-ago:** Kevin es owner, no ejecutor. Pavel ya hace la validación SEO en W-016, así que él trae el nicho con evidencia y Kevin lo aprueba junto con el dominio en W-007. **Avance 2026-09-03:** el nicho llegó directo de Kevin por el chat de Zoom (homeowners insurance en Stuart, alto valor / frente al agua / costero), no por el flujo planeado de Pavel proponiendo con evidencia primero. Ver la nota de riesgo en W-007 |
| W-004 | Nicho y mercado en español para sitio #2 | **Pavel propone**, Kevin aprueba | 2026-08-28 | Igual que W-003 |
| W-005 | Número concreto para Gate B | Kevin **aprueba**, Vic propone | 2026-08-28 | Gate B es indecidible sin él. **El modelo ya está en `docs/GATE_B_MODEL.md`**: Kevin solo llena tres celdas (comisión promedio por póliza, tasa de cierre de llamada a venta, y qué retorno anual por sitio justifica seguir). Un número que hay que aprobar se aprueba; uno que hay que inventar se pospone hasta que los datos lo inventen solos |
| W-100 | **Definir qué cuenta como "llamada calificada" y cómo se registra** | Kevin + Vic | 2026-09-25 | **Hueco encontrado el 26-ago.** El bar de Gate B se mide en llamadas calificadas y nadie ha dicho qué califica. La definición determina qué tiene que capturar el tracking, y el tracking se cablea antes del launch del 9 de octubre. Incluye disposición de llamadas en GoTo y etiquetado semanal, no trimestral: nadie clasifica 120 días de llamadas de memoria en marzo. **Avance 2026-09-10:** la definición de partida ya existía, escrita dentro de `docs/GATE_B_MODEL.md` desde el 26-ago, sin que nadie la hubiera separado ni mandado a aprobar. Se sacó a un documento propio, `docs/QUALIFIED_CALL_DEFINITION.md`, mismo patrón que W-005 (Kevin aprueba o edita, no inventa desde cero). Falta: que Vic se lo mande a Kevin, y nombrar quién hace el etiquetado semanal (candidatos: Pavel, o quien hoy atienda la línea de Walker) **Avance 2026-09-21:** texto final aprobado por Kevin (11-sep: sin exclusión por área de servicio; repetidas deduplicadas sobre la ventana de 120 días) **ya escrito** en `docs/QUALIFIED_CALL_DEFINITION.md` (v1.1) y `docs/GATE_B_MODEL.md` (v1.1), los dos sincronizados. **Sigue abierto solo:** nombrar quién hace el etiquetado semanal (candidatos: Pavel, o quien atienda la línea de Walker), y encender la mecánica en GoTo, que es W-024. |
| W-006 | ¿Los sitios piloto tienen su propio Google Business Profile? | Kevin | 2026-08-28 | Puede cambiar qué nichos son viables. **Vencido desde el 2026-08-28; señalado de nuevo el 2026-09-04** al revisar contra el plan maestro: ya se aprobó nicho y dominio para el sitio #1 sin esta respuesta, y es justo la decisión que el documento original marca como capaz de cambiar qué nichos son viables. Entre más tarde llegue, menos margen queda para cambiar de nicho si la respuesta lo exige |
| W-007 | Dominios aprobados para sitios #1 y #2 | Kevin | 2026-09-04 | W-011. Se aprueban DESPUÉS de W-016. **Avance 2026-09-03:** Kevin aprobó el dominio del sitio #1 por el chat de Zoom: **StuartHomeownersInsurance.com, ya comprado** ("we own it"), con el nicho asignado directo por él: homeowners insurance en Stuart, enfocado en propiedades de alto valor, frente al agua y costeras. Falta el dominio y nicho del sitio #2. **Riesgo a resolver, no a ignorar:** no consta que W-016 (validación SEO de Pavel) haya corrido antes de esta aprobación, que es exactamente el orden que fija `docs/ARCHITECTURE.md` para no comprar en un mercado dominado por agregadores sin ángulo local. El nicho que llegó ya es específico, lo que mitiga parte del riesgo, pero no lo reemplaza. No bloquea seguir, pero Pavel debería correr W-016 en retroactivo esta semana, antes de invertir tiempo de contenido real: barato ahora, caro en Gate B. **Avance 2026-09-04:** se armó un demo de `sites/stuart-homeowners` con este nicho y contenido de marcador de posición (marca, teléfono, email y license number claramente marcados como pendientes), para mostrarle a Kevin el template funcionando ahora que el dominio ya está en Cloudflare, sin que W-016/W-008 estén cerrados todavía. Se despliega solo al subdominio de preview (`workers.dev`), sin ruta al dominio custom, precisamente para no publicar nada con datos falsos en `stuarthomeownersinsurance.com`. **Deploy confirmado el 2026-09-04:** https://wicfl-stuart-homeowners-demo.wicfl-microsites.workers.dev/ — pendiente que Vic se lo mande a Kevin **Avance 2026-09-22:** Kevin anunció por Zoom el dominio del Sitio #2: **`portsaintluciehomeinsurance.com`**. Verificado por WHOIS: registrado hoy en **GoDaddy** (segunda vez fuera de Cloudflare Registrar, que es lo que fija `docs/ARCHITECTURE.md`), nameservers de GoDaddy. Se conecta igual que Stuart (cambio de nameservers). Pregunta abierta a Kevin: si seguirá comprando él los dominios o los compramos en Cloudflare; afecta si la Fase 7 automatiza compra de dominios. El nombre en inglés implica que el Sitio #2 **no** es el sitio en español del plan; falta que Kevin lo confirme explícitamente (ver W-031). |
| W-093 | ~~**Datos de pago de la empresa** para el billing de Cloudflare y GoTo~~ **Cerrado el 2026-09-03.** Kevin confirmó por el chat de Zoom: "Microsite payment method complete." **Acotado el 26-ago:** el vault lo monta Vic; con el buzón ya creado (W-092), Vic creó la cuenta y le pasó a Kevin la pantalla de billing para que capturara la tarjeta él mismo | Kevin | — | Ya no bloquea nada |
| W-094 | ~~¿Walker ya tiene org de GitHub?~~ **Cerrado el 26-ago: la creamos nosotros.** Vic la levanta bajo el correo de W-092. Ver W-012 | Vic | — | Ya no bloquea nada |

---

## Fase 1 — Infraestructura y validación (31 ago – 4 sep 2026)

| id | Item | Dueño | Notas |
|---|---|---|---|
| W-010 | Crear cuenta Cloudflare con `microsites@wicfl.com`, 2 Super Admins, Pavel como Administrator, 2FA, recovery codes en vault compartido | Vic | **Desbloqueado el 2026-09-01.** Ver `docs/ACCOUNTS_AND_ACCESS.md`. **Primer paso, antes de crear nada: rotar la contraseña del buzón.** Llegó por el chat de Zoom, donde queda en el historial de forma indefinida y buscable, visible para cualquiera que se agregue al hilo después. La nueva no se comparte por chat: vive en el vault, junto con los recovery codes del 2FA |
| W-105 | **Montar el vault compartido** (Bitwarden o 1Password) y meter ahí la credencial rotada del buzón y los recovery codes | Vic | **Urgente desde el 2026-09-01:** hasta que exista, la única copia de las credenciales del proyecto está en un chat. Precede a W-010. **Avance 2026-09-04:** esto no lo puede montar cowork — crear la cuenta del vault y mover contraseñas ahí son acciones que tiene prohibidas siempre, sin excepción. Se dejó en cambio `docs/VAULT_SETUP_CHECKLIST.md`: qué vault elegir, qué entradas crear, en qué orden (primero rotar el buzón, después las credenciales de Cloudflare que trae W-010), y cómo nombrarlas, para que el montaje real sea copiar y pegar. Sigue pendiente de que Vic lo ejecute; se cierra cuando exista el vault con al menos la entrada del buzón rotado adentro |
| W-012 | ~~Crear org de GitHub y monorepo, configurar accesos~~ | Vic | **Cerrado 2026-09-02.** `main` está remoto y es la rama por defecto; `ci.yml` pasó verde en el run `33665589246` (push). Prompt: `prompts/2026-09-02_005_arreglar-ci.md`; reporte: `reports/2026-09-02_005_arreglar-ci.md`; commit: `de4a5ef` (`fix(ci): quote the ajv glob and normalize line endings`). La organización usa `microsites@wicfl.com` como Owner y personas con sus propias cuentas. |
| W-013 | Configurar zonas, DNS y SSL | Vic | **Desbloqueado el 2026-09-04.** Zona de `StuartHomeownersInsurance.com` activa en Cloudflare (ver W-011). Falta: revisar el modo de encriptación SSL/TLS antes de apuntar el dominio a un Worker real, y decidir proxy status de los registros A una vez se reemplace el parking de GoDaddy |
| W-106 | ~~`.gitattributes` que normalice finales de línea a LF~~ | Vic | **Cerrado 2026-09-02.** `.gitattributes` normaliza texto a LF y protege `*.xlsx` como binario; `git diff --cached --ignore-all-space` quedó vacío tras renormalizar. Prompt: `prompts/2026-09-02_005_arreglar-ci.md`; reporte: `reports/2026-09-02_005_arreglar-ci.md`; commit: `de4a5ef` (`fix(ci): quote the ajv glob and normalize line endings`). |
| W-014 | ~~Levantar pipeline de deploy con Workers Static Assets + GitHub Actions~~ **Cerrado el 2026-09-04.** **Avance 2026-08-31:** CI, deploy y preview workflows escritos en `1c37278`; deploy y preview siguen desactivados hasta W-092/W-010. Prompt: `prompts/2026-08-28_002_monorepo-y-pipeline.md`; reporte: `reports/2026-08-28_002_monorepo-y-pipeline.md`. Incluye los preview deploys por rama de W-098. Montarlos después cuesta el doble. **Bug encontrado el 2026-09-02 al ver correr CI por primera vez:** `npm run check` falla en Linux con `too many arguments`, porque el script pasa `-d packages/config-schema/examples/*.json` sin comillas y bash expande el glob antes de que AJV lo vea. En Windows no se notaba porque la shell no expande ese patrón. El comando de validación diario del proyecto estaba roto en la única plataforma donde siempre va a correr. Corregido en `de4a5ef`; CI verde en el run `33665589246`. **Observación pendiente del 2026-09-02:** los filtros de `ci.yml` y `deploy.yml` no coinciden. `ci.yml` incluye `package.json` y `package-lock.json` y `deploy.yml` no, así que subir la versión de Astro pasaría CI y nunca dispararía deploy aunque el HTML cambie; al revés, `deploy.yml` incluye `scripts/**` y `ci.yml` no, así que un cambio del generador desplegaría sin validar. No muerde hoy porque deploy está desactivado; muerde en octubre. Se resuelve al cerrar este item, cuando el build por sitio de W-026 defina los filtros definitivos. **Avance 2026-09-03:** `CLOUDFLARE_API_TOKEN` y `CLOUDFLARE_ACCOUNT_ID` ya están en GitHub y se verificaron de punta a punta con un deploy de prueba desechable (fixture `_example`, borrado después de confirmar). En el camino se encontró y corrigió un bug real: a `deploy.yml` y `preview.yml` les faltaba el bloque `env:` que pasa esos dos secrets a `wrangler deploy`, así que ninguno de los dos iba a autenticar aunque se activaran. Commits `997ae7a` y `8cfe5ac`, sin prompt/reporte formal — ver `BITACORA.md` 2026-09-03. Sigue sin cerrarse: falta el `wrangler.toml` real con nombres de Worker y mapeo de pods, que depende de que se aprueben los dominios en W-007/W-011. **Avance 2026-09-04:** montado el `wrangler.toml` real con mapeo de pods, siguiendo la decisión ya escrita en `docs/ARCHITECTURE.md` (pods de ~25 sitios por Worker, no un Worker por sitio). `pods/pod-1.json` define qué slugs va cada pod (hoy solo `stuart-homeowners`); `scripts/build-pod.mjs` construye cada sitio del pod y arma `dist/pods/<pod>/`; `scripts/pod-worker-template.mjs` genera el Worker que enruta por dominio (`Host` header → slug → `env.ASSETS.fetch()`), porque un solo binding de Static Assets solo sirve un árbol de directorios y hacía falta un script que eligiera cuál sitio responde. Probado con un test standalone (mock de `ASSETS.fetch`): enruta bien por apex, `www` y mayúsculas/minúsculas del host, y devuelve 404 en un host no configurado. De paso se corrigió también la observación pendiente del 2-sep: `deploy.yml` ya incluye los mismos filtros de ruta que `ci.yml` (`package.json`, `package-lock.json`, `packages/config-schema/**`), así que un cambio de dependencias ya no puede pasar CI en verde y saltarse la validación antes de un deploy real. `deploy.yml` sigue en `if: false`: `sites/stuart-homeowners` todavía tiene licencia, contacto y analytics de mentira mientras es el demo de Kevin, y el gate de W-103 lo rechaza — verificado que efectivamente lo rechaza. Se activa cuando esos datos sean reales y Vic decida lanzar. Commit `6298138`, sin prompt/reporte formal | Vic | **Avance 2026-09-21:** el camino a producción nunca se ha ejercitado contra un dominio real, y `wrangler.pod-1.toml` engancha el apex en el primer deploy que corra. Se abrió **W-119** (prompt `prompts/2026-09-21_015_ensayo-deploy-produccion.md`): ensayo en `preview.stuarthomeownersinsurance.com` con un Worker separado, `deploy.yml` activado solo por `workflow_dispatch` con confirmación, y `docs/LAUNCH_RUNBOOK.md` para el 9-oct. Este ítem cierra cuando el sitio #1 esté en vivo en el apex. **Avance 2026-09-22:** deploy.yml ya está activo exclusivamente por dispatch, con confirmación y targets rehearsal/production; el ensayo expuso que el token actual no puede crear rutas de zona. |
| W-015 | ~~Cerrar `docs/ARCHITECTURE.md` en su versión de handoff~~ **Cerrado el 2026-09-04.** Documentada la excepción del dominio #1 (GoDaddy en vez de Cloudflare Registrar), corregida la afirmación de ruteo bilingüe para que coincida con la corrección de `docs/SCHEDULE.md` (construido y ejercitado, ningún piloto lo usa), agregado el gate de producción (W-103), y añadida una nota de estado que distingue lo que existe hoy de la automatización de Fase 7 que describe el diagrama principal, para que Pavel no lo lea como la realidad de hoy en el handoff. Commit `a06fba1`, sin prompt/reporte formal — ver `BITACORA.md` 2026-09-04 | Vic | |
| W-016 | **Validación SEO del nicho antes de comprar dominio**: keywords, search intent, competencia en SERP, ángulo local real | Pavel | *(aporte de Pavel)* Si el nicho está dominado por agregadores sin ángulo local, se descarta y Kevin elige otro. **Bloquea W-007 y W-011.** **Riesgo elevado el 2026-09-04, al revisar contra el plan maestro:** el nicho y el dominio del sitio #1 ya se aprobaron (W-007, 3-sep) sin que conste que esta validación haya corrido. Es exactamente la secuencia que el plan existe para proteger — comprar antes de validar es el riesgo de doorway pages del documento original, no un detalle de proceso. Correrla en retroactivo esta semana, antes de que Pavel invierta tiempo de contenido real, sigue siendo barato; en Gate B con contenido ya publicado no lo sería **Avance 2026-09-21:** para Stuart ya es retroactivo y el contenido está escrito. **La corrida que sí importa ahora es Port St. Lucie:** Pavel empieza a escribir el sitio #2 el 12-oct; la validación de PSL (keywords, intent, SERP, ángulo local) va esta semana, antes de la primera página. Es exactamente la secuencia que el plan diseñó y no se ejecutó para el sitio #1. |
| W-017 | **Provisionar accesos a Pavel**: Cloudflare Administrator, GitHub, GoTo, GA4, Search Console | Vic | **Movido al 31 de agosto.** Pavel arranca de tiempo completo ese día; esperar accesos hasta el 2 de septiembre son dos días perdidos. **Aclarado el 2026-09-04 por Vic:** Cloudflare Administrator y GitHub son autoservicio, Vic las armó bajo `microsites@wicfl.com` (W-010, W-012) y puede invitar a Pavel sin depender de nadie. **GoTo y GA4 son cuentas que ya existen y son de Kevin**, no cuentas nuevas del proyecto, así que el acceso ahí sí depende de que Kevin invite. No urgente todavía: Pavel no empieza a trabajar hasta el 21 de septiembre (Fase 3), así que no hay que pedírselo a Kevin ya, pero queda anotado para no perder de vista de quién depende cada uno **Corrección 2026-09-21:** Vic confirma que **sí tiene acceso a GoTo**; la nota de arriba que lo ponía como dependencia de Kevin queda sin efecto para GoTo. GA4 y Search Console se crean bajo `microsites@wicfl.com` (W-025), tampoco dependen de Kevin. Lo único que sigue en Kevin de este ítem: nada; los accesos de Pavel los da Vic cuando Pavel los necesite (Cloudflare Administrator antes de W-112). |
| W-011 | ~~Registrar dominios de sitios #1 y #2 vía Cloudflare Registrar~~ **Nameservers de `StuartHomeownersInsurance.com` cambiados a Cloudflare el 2026-09-04, propagación casi instantánea.** El dashboard de Cloudflare confirma "Your domain is now protected by Cloudflare" (SSL/TLS y proxy activos). Sigue registrado en GoDaddy, no se movió el registro en sí, solo el DNS. Domain #2 sigue pendiente | Vic | Depende de W-007, que ahora depende de W-016. **Nota 2026-09-03:** Kevin ya compró/posee `StuartHomeownersInsurance.com`, confirmado registrado en **GoDaddy**, no en Cloudflare Registrar como fija el plan original (`docs/ARCHITECTURE.md`). Kevin le dio a Vic acceso de delegado a su cuenta de GoDaddy el 2026-09-04 (vía Delegate Access, sin compartir contraseña), así que de aquí en adelante Vic puede cambiar nameservers y gestionar DNS directo, sin pedírselo a Kevin cada vez. **Pendiente de limpieza, no urgente:** los 2 registros A siguen apuntando al parking de GoDaddy, reemplazarlos cuando el Worker real esté listo para este dominio (W-013/W-014); borrar el CNAME `_domainconnect` que ya no sirve. Pendiente para la próxima vez: comprar dominios futuros directo en Cloudflare Registrar para saltarse este paso |
| W-108 | ~~**Refrescar `docs/master-file-source.html` contra `BACKLOG.md` y republicar**~~ **Cerrado el 2026-09-10.** El bloqueo de red del allowlist ya no aplicaba; se pudo leer y republicar directo. Subido a v1.6: cierra en la tabla de Open Items los tres puntos que ya estaban resueltos desde v1.5 (revisor de contenido, cuenta de GitHub, CRM), suma el nicho/dominio de inglés como resuelto, actualiza el estado de los assets de marca (logo entregado, license/contacto todavía pendientes de Kevin), corrige la fecha de Gate B en el pie (ya decía 5-mar en el cuerpo pero 12-feb en el footer), y refresca la franja de estado (fase actual, conteo de pendientes de Kevin). Publicado en `https://claude.ai/code/artifact/b1c34949-479b-48f6-a269-8522d4b2aa82`. Pendiente: commitear el `docs/master-file-source.html` actualizado al repo (ver BITACORA 2026-09-10) |
| W-109 | **Soporte de logo en el schema y el template** | Vic | **Cerrado el 2026-09-04**, mismo día que se abrió. Nació de la pregunta de Pavel en W-008: el template no tenía ningún campo ni gancho para un logo, solo texto. Se agregó `brand.logo` opcional al schema (ruta relativa como `/logo.svg`, sin romper ningún config existente porque es opcional), se actualizó `BaseLayout.astro` para renderizar `<img>` cuando existe y el texto de siempre cuando no, y se extendió `build-site.mjs` para copiar `sites/<slug>/public/` sobre el output del build, así que el archivo del logo vive dentro del propio directorio del sitio del operador, no en el config ni en el template compartido. Probado de punta a punta con un SVG de prueba en `sites/_example/public/logo.svg` (queda ahí como caso vivo, mismo patrón que el fixture bilingüe) y verificado que `stuart-homeowners`, que no tiene el campo, sigue renderizando el wordmark de texto exactamente igual que antes. `npm run check` y el build del pod completo, verdes. Documentado en `docs/SITE_CONFIG_SCHEMA.md` (decisión de diseño 7) y `docs/OPERATOR_GUIDE.md`. Lo único que falta para usarlo de verdad es que Kevin entregue el logo (W-008) |

---

## Track paralelo — Rampa técnica de Pavel (31 ago – 18 sep 2026)

| id | Item | Dueño | Notas |
|---|---|---|---|
| W-102 | **Guía del operador + sesión de orientación con Pavel** | Vic | **Abierto el 28-ago.** Pavel se ve confundido y el diagnóstico no es técnico: su propio documento proponía contenido generado desde briefs y sitios construidos a mano, o sea que se imaginaba un trabajo distinto al que el framework asume. Una capacitación de herramientas no arregla eso. Va en dos tiempos: **orientación ahora** (qué es un sitio aquí, qué toca él, qué no toca nunca, cómo se ve una semana suya), sin herramientas y sin depender del schema; y **capacitación técnica después**, cuando existan el schema y el repo, que es material de W-028 adelantado, no trabajo extra **Avance 2026-09-04:** escrita `docs/OPERATOR_GUIDE.md`. Cubre las dos partes que este ítem pedía, ahora que el schema y el repo ya existen: la orientación (qué es este repo, qué toca Pavel, qué nunca toca, cómo se ve su semana) nombrando directo las dos señas de identidad de su propio documento que chocan con el framework real — contenido generado desde briefs, y sitios construidos a mano — sin tratarlas como un error suyo, porque nadie se lo había dicho antes; y la guía técnica operativa (los cinco pasos de una semana real, qué documento abrir para qué, las reglas que no se doblan), que apunta a `docs/SITE_CONFIG_SCHEMA.md`, `docs/CONTENT_STANDARDS.md` y `docs/QA_CHECKLIST.md` en vez de repetirlos. De paso se actualizó `docs/SETUP.md`, que seguía diciendo que los preview deploys estaban desactivados cuando ya están en vivo desde W-098, y se agregaron ambos al índice de `docs/README.md`. **No se cierra todavía:** falta que Pavel la lea y que la sesión de orientación en vivo pase (eso es W-028, sigue abierto); el criterio de cierre real de este ítem es si la guía responde sus preguntas sin que Vic tenga que explicarlo de nuevo, y eso no se sabe hasta que Pavel la use. **Avance 2026-09-08:** Vic leyó la guía y encontró exactamente el tipo de hueco que este ítem esperaba que alguien encontrara: asumía que Pavel ya sabía usar GitHub y no explicaba ni el mecanismo (editar/commitear/pull request/merge) ni cómo crear una página nueva. Se agregó un Part 2 completo a `docs/OPERATOR_GUIDE.md` (v1.1) con los pasos verificados en vivo, botón por botón, para ambos casos, más un glosario en lenguaje llano y qué hacer si un check falla. Sigue sin cerrarse por la misma razón de siempre: el criterio real es que Pavel la use sin que Vic tenga que explicarlo de nuevo,  **Avance 2026-09-21:** Pavel arrancó su trabajo real y le pidió a Vic una junta para revisar sus tareas; Vic la reemplazó por un video grabado. Cowork escribió el guión completo (`pavel-video-tutorial/guion.md`) y publicó un artifact con las 12 capturas reales del camino de GitHub embebidas: https://claude.ai/artifact/8FSAzBZB42tBjdPgTR5WTQ. Faltan 5 capturas del formulario (`content-form`), pendientes de que Vic inicie sesión — cowork no puede escribir la contraseña. Sigue sin sincronizarse `docs/OPERATOR_GUIDE.es.md` (v1.3) con el artifact del manual del operador (v1.4). Ver `BITACORA.md` 2026-09-21 (cierre del día). No se cierra todavía: falta que Pavel vea el video y que su uso real confirme si responde sus preguntas sin que Vic tenga que explicarlo de nuevo. |


| id | Item | Dueño | Notas |
|---|---|---|---|
| W-018 | Rampa: HTML/CSS para leer y diagnosticar, conceptos de Astro, workflow de Git, Cloudflare básico | Pavel | Alcance acotado: **no necesita escribir HTML**. Con dedicación completa confirmada el 25-ago, la ventana 31 ago – 18 sep es realista y la Fase 3 arranca el 21 de septiembre como estaba |
| W-019 | Sombra durante la construcción del framework (Fase 2) | Pavel | 8–18 sep, confirmado |

---

## Fase 2 — Framework (8–18 sep 2026) · Vic con Pavel de sombra

Diez items en nueve días hábiles no cabe, sobre todo con Pavel de sombra restando velocidad.
Lo que hicimos no es recortar alcance, es **separar por deadline real**: no todo lo de Fase 2
está amarrado al handoff del 18 de septiembre. La mitad está amarrada al launch del sitio #1,
el 9 de octubre, y Pavel puede construirla durante la Fase 3.

### Bloque A — obligatorio para el handoff del 18 de septiembre

Sin esto Pavel no puede empezar el sitio #1 el 21 de septiembre.

| id | Item | Dueño | Notas |
|---|---|---|---|
| W-020 | ~~Definir el `site.config.json` schema + validación~~ | Vic + Pavel | Cerrado 2026-08-31. Prompt: `prompts/2026-08-25_001_site-config-schema.md`; reporte: `reports/2026-08-25_001_site-config-schema.md`; commit: `ed3d425` (`feat(schema): close site.config.json contract with validation and examples`). Pavel revisa y rompe antes de congelarlo operacionalmente. |
| W-107 | ~~Renderer de markdown real, con content collections de Astro~~ | Vic | **Cerrado 2026-09-02.** Astro content collections cargan markdown externo de cada sitio mediante el entorno del generador, con frontmatter obligatorio y render completo. CI verde: run `33670312569`. Prompt: `prompts/2026-09-02_007_template-y-markdown.md`; reporte: `reports/2026-09-02_007_template-y-markdown.md`; commit: `478d7b2` (`feat(template): real markdown pipeline and design system`). |
| W-021 | ~~Template base de Astro: layouts, componentes, design system~~ **Cerrado 2026-09-18 tras fix de CI.** El primer cierre (`80dbd57`) falló en GitHub Actions porque el runner limpio no tiene `@types/node`: `astro check` reportó que `content.config.ts` no podía tipar `process` ni `node:url`. `8de5a04` añadió declaraciones mínimas de esas APIs usadas, sin dependencias ni relajación de TypeScript. [Run 35391044422](https://github.com/microsites-wicfl/wicfl-microsites/actions/runs/35391044422) verde: `npm run check` pasó en 18 segundos y los builds de `_example` y `stuart-homeowners` pasaron. Prompt y reporte: `prompts/2026-09-10_011_cerrar-astro-check.md`, `reports/2026-09-10_011_cerrar-astro-check.md`. | Vic / Codex |
| W-022 | ~~Ruteo bilingüe en/es con `hreflang`~~ **Cerrado el 2026-09-04.** Solo los locales no-primarios llevan prefijo de URL, así que ningún sitio piloto real cambia de ruta (ambos tienen `alternates: []`). Un locale alternativo vive en `content/<locale>/`; los enlaces `hreflang` (más `x-default`) se calculan por página contra la colección completa y se inyectan en el `<head>` de `BaseLayout`, con `<html lang>` real por página en vez de fijo al primario. Ejercitado de punta a punta dándole a `sites/_example` (ya configurado `alternates: ["es"]` desde antes) contenido real en español para home y about, dejando coverage-fixture a propósito solo en inglés para probar también el caso sin contraparte. Commit `b46f972`, sin prompt/reporte formal — ver `BITACORA.md` 2026-09-04 | Vic | Se construye ahora, no se retrofitea. **Hallazgo del 31-ago:** con el schema cerrado queda claro que **ningún piloto lo ejercita**: el sitio #1 es `en` sin alternates y el #2 es `es` sin alternates, o sea dos sitios monolingües independientes, no versiones uno del otro. `docs/SCHEDULE.md` dice que el sitio #2 valida el soporte bilingüe y **no es cierto**: valida contenido en español, que es otra cosa. Se construye igual porque define el ruteo del template, pero hay que ejercitarlo con el sitio `_example` del prompt 002 configurado bilingüe, o queda como código muerto que se estrena a escala |
| W-026 | ~~Comando `new-site`: genera un sitio desde su config, **y la matriz de sitios cambiados en CI**~~ | Vic | **Cerrado 2026-09-02.** `npm run build:site -- <site-directory>` valida el config, genera `dist/sites/<site-directory>/` desde el template único y consume markdown de ese sitio. CI valida globalmente y construye por matriz los sitios cambiados; cambios compartidos reconstruyen todos. CI verde: run `33667480670`. Prompt: `prompts/2026-09-02_006_generador-new-site.md`; reporte: `reports/2026-09-02_006_generador-new-site.md`; commit: `0700bde` (`feat(generator): build a site from its config`). |
| W-098 | ~~**Preview deploy por rama**: Pavel hace push de markdown y recibe una URL con su sitio renderizado~~ | Vic | **Cerrado el 2026-09-08**, repitiendo la prueba real del PR después de que W-110 sincronizó `main` con GitHub. PR #3, mismo cambio de una línea en `sites/_example/content/index.md` editado sin terminal: esta vez `Preview deploy` **no se saltó** — corrió, desplegó a `wicfl-pr3-_example.wicfl-microsites.workers.dev`, y comentó la URL en el PR. Se visitó la URL y mostraba el contenido editado, verbatim. Al cerrar el PR, `preview-cleanup.yml` borró el Worker — se confirmó visitando la misma URL de nuevo y recibiendo 404 de Cloudflare. Las cinco piezas prometidas (descubrir → validar → construir → desplegar → comentar → limpiar) quedaron verificadas de punta a punta contra el repo real, no contra una copia local. PR de prueba cerrado y rama borrada. Con esto, el camino de respaldo para Pavel (editar en github.com, sin terminal) queda confirmado funcional y ya se puede mandar `docs/OPERATOR_GUIDE.md` / el artifact de la guía sin reservas técnicas. |
| W-110 | ~~Sincronizar `main` local con `origin/main` en GitHub~~ | Vic | **Cerrado 2026-09-08.** Confirmado fast-forward limpio y empujado sin force: `origin/main` avanzó de `09732ab` a `03f2ff2`, idéntico a `main` local. `Validate and build` verde: [run 34252815726](https://github.com/microsites-wicfl/wicfl-microsites/actions/runs/34252815726). Prompt: `prompts/2026-09-08_009_sincronizar-main-con-origin.md`; reporte: `reports/2026-09-08_009_sincronizar-main-con-origin.md`; commit: `24f0db8` (`docs: close W-110 after syncing main`). W-098 sigue requiriendo repetir su prueba de PR real, ahora contra el workflow sincronizado. |
| W-111 | **Formulario mínimo de carga de contenido para Pavel, sin conceptos de git** | Vic | **Avance 2026-09-11:** implementado en `apps/content-form/` como Worker aislado: descubre cualquier `sites/*/site.config.json`, edita Markdown y solo `brand.name`/`seo`, crea rama y PR mediante GitHub API, sin auto-merge ni cambios al pipeline. Validado con `node --check`, `npm run check` y `wrangler deploy --dry-run`. **Avance 2026-09-15:** desplegado y probado con login real. Cambio de plan respecto al 11-sep: se desplegó por la integración nativa de Cloudflare con GitHub (auto-deploy al pushear a `main`), no con `wrangler deploy` por CLI como decía el README original; los secretos (`GITHUB_TOKEN`, `OPERATOR_PASSWORD`) se cargaron desde el dashboard de Cloudflare, no con `wrangler secret put`. El token limitado de Cloudflare que se había preparado para el flujo por CLI **no se usó** y sigue activo sin propósito — pendiente que Vic lo revoque. Tres bugs reales aparecieron al probar de punta a punta y ya están corregidos: botón de login no reaccionaba por JS inválido servido por el Worker (`06ef3a1`), GitHub devolvía 403 por falta de header `User-Agent` en las llamadas del Worker (`68a7c5c`), la validación rechazaba el guion bajo de `_example` (`bf5a3d0`). **Sigue abierto:** falta confirmar la prueba real de punta a punta contra `_example` (PR creado, `ci.yml`/`preview.yml` corridos, comentario de preview, PR de prueba cerrado y su rama borrada), y falta que `apps/content-form/README.md` y el reporte reflejen el mecanismo de deploy real (hoy documentan el flujo por CLI que ya no se usa). Reporte: `reports/2026-09-08_008_formulario-carga-contenido.md` (desactualizado respecto al 15-sep, ver bitácora). **Avance 2026-09-17:** prueba real de punta a punta confirmada — PR de prueba creado contra `sites/_example`, `Validate and build` y `Preview deploy` en verde, comentario de preview publicado, verificado por navegador contra el repo real. **No se cierra todavía:** falta que Vic cierre ese PR de prueba y borre su rama (sin acceso de escritura a GitHub desde el bridge), y siguen sin actualizarse `apps/content-form/README.md` y este mismo reporte al mecanismo real de deploy (integración Git de Cloudflare, no `wrangler deploy` por CLI). |
| W-112 | Documentar cómo se compra y conecta un dominio nuevo (Pavel, sin ayuda, antes de Gate A) | Vic | **Abierto el 2026-09-08,** a partir de una pregunta directa de Vic tras revisar la guía del operador: ¿cómo hace Pavel para crear un dominio nuevo y conectarlo al sitio, y eso está en la guía? No está, y hoy no hace falta que esté: el dominio del Sitio #1 (`StuartHomeownersInsurance.com`) ya lo compró Kevin en GoDaddy y lo conectó Vic a Cloudflare (W-011/W-013), así que el trabajo de Pavel para el 17-sep y el 9-oct sigue siendo solo contenido. **Pero `docs/SCHEDULE.md` ya deja escrito, en la definición del propio Gate A (13-nov, Sitio #3), que ahí Pavel elige el nicho Y compra el dominio, sin ayuda:** "if Pavel picks the niche, Pavel buys the domain." Eso hoy es un hueco real, no solo de documentación: Pavel todavía no tiene su acceso de Administrator en Cloudflare (ver W-017, abierto, no urgente hasta el 21-sep), y nadie ha probado en vivo el flujo de comprar un dominio por **Cloudflare Registrar**, que es el camino que `docs/ARCHITECTURE.md` fija como estándar para dominios futuros (GoDaddy fue una excepción puntual solo para el dominio #1, porque Kevin ya lo tenía comprado ahí antes del plan). Antes de escribir cualquier paso a paso para Pavel, hace falta decidir con Vic qué camino se documenta y probarlo en vivo igual que se hizo con GitHub, para no inventar botones de una pantalla que nadie ha usado. **No bloquea el handoff del 17-sep ni el lanzamiento del 9-oct.** Bloquea Gate A (13-nov) si se deja para el final. Depende de que W-017 (acceso de Pavel a Cloudflare) esté cerrado para que Pavel pueda ejecutarlo él mismo |
| W-113 | ~~Invitar a Pavel al repo de GitHub~~ (hecho) y confirmar si `microsites-wicfl` debe seguir siendo cuenta personal o convertirse en organización | Vic | **Abierto el 2026-09-08.** Verificado en vivo hoy, en el Chrome real de Vic: `microsites-wicfl` es una **cuenta personal de GitHub** (`@microsites-wicfl`), no una organización, `github.com/orgs/microsites-wicfl/people` da 404. Esto contradice lo que dice la nota de cierre de **W-012**: "la organización usa `microsites@wicfl.com` como Owner y personas con sus propias cuentas." No es organización, así que invitar a Pavel no es "agregar miembro a la org", es ir al repo, Settings, Collaborators and teams, Add people, con su usuario de GitHub, dándole **Write** (no Admin). **Avance 2026-09-09:** Vic confirma que Pavel ya tiene acceso al repo. **Ya no bloquea que Pavel opere en el repo directamente.** **Sigue abierto, sin urgencia:** decidir si vale la pena migrar de cuenta personal a organización antes de sumar más gente, una org da 2FA obligatorio a nivel de organización, roles más finos que solo Write/Admin, y un log de auditoría que una cuenta personal no tiene. No es urgente si van a seguir siendo pocas personas, pero es una decisión de gobierno del mismo tipo que ya está escrita para Cloudflare en `docs/ACCOUNTS_AND_ACCESS.md`, y hoy no está tomada ni documentada para GitHub. |
| W-029 | **La lista** del checklist de QA y launch: técnico, SEO, contenido, mobile, conversión, tracking, deploy | Pavel + Vic | *(aporte de Pavel)* Es la definición de "listo para publicar" de Pavel. Si no existe el 21 de septiembre, escribe sin saber contra qué y la lista se acaba escribiendo el 8 de octubre para empatar con lo que ya construyó. Medio día, sobre todo trabajo suyo. **Avance 2026-09-04:** Vic escribió un borrador completo en `docs/QA_CHECKLIST.md` (7 categorías, marca qué está automatizado vs manual vs sin construir todavía), para que Pavel llegue a revisar y ajustar en vez de partir de cero. **No se cierra todavía:** sigue pendiente que Pavel lo revise y lo haga suyo, que es el trabajo real que este item pide |
| W-028 | Sesión de handoff con Pavel + documentación escrita | Vic + Pavel | Criterio de salida: Pavel genera un sitio vacío sin ayuda. **Correrlo el 17, no el 18**, para que una falla deje un día |

### Bloque B — obligatorio para el launch del sitio #1, 9 de octubre

Se construye durante la Fase 3, con Pavel ya operando. Si alguno se desborda, se recorre
dentro de octubre sin tocar el handoff ni el 21 de septiembre.

| id | Item | Dueño | Notas |
|---|---|---|---|
| W-023 | ~~SEO técnico: sitemap, robots, canonical, schema.org InsuranceAgency~~ **Cerrado el 2026-09-21.** El template genera canonical por ruta real, `robots.txt`, `sitemap.xml` y JSON-LD `InsuranceAgency` con el NAP del config para cada sitio, sin trabajo adicional de Pavel. Ver prompt `prompts/2026-09-10_010_seo-tecnico.md`, reporte `reports/2026-09-10_010_seo-tecnico.md`, commit `b1b3133` y CI verde [run 35601811392](https://github.com/microsites-wicfl/wicfl-microsites/actions/runs/35601811392). | Vic | El template del Bloque A no debe impedirlo |
| W-024 | Número de tracking como campo de config, ruteado a GoTo | Vic | GoTo confirmado a $0.99/número **Avance 2026-09-21 (plan de next steps acordado con Vic):** cero avance técnico hasta hoy, y es lo que le da sentido a la fecha del launch: el reloj de Gate B arranca el 9-oct y sin DID con grabación o disposición encendida los primeros días de datos no existen. Vic tiene acceso a GoTo (ver W-017), así que **esta semana (22–26 sep) Vic compra el DID, lo enruta y enciende grabación/disposición**, sin depender de Kevin. El número va a `contact.trackingPhone`/`displayPhone` en el config junto con los datos reales de W-008. **Avance 2026-09-22:** Kevin anunció el teléfono del Sitio #2 (PSL): **1 772 335 4779**. Pendiente confirmar con Kevin que es un DID nuevo comprado en GoTo solo para PSL (si es una línea existente de Walker no sirve como tracking: mezcla llamadas y Gate B no se puede medir). El de Stuart sigue sin llegar. |
| W-101 | Alta de la sub-account de GoHighLevel para WICFL, con credenciales de API y mapeo de campos del lead | Vic | Sale de cerrar W-096. Necesita el correo de W-092. Bloquea W-025. **Avance 2026-09-08:** Kevin confirmó por chat que en vez de una sub-account nueva se usa la sub-account "Walker Insurance" que ya existe, porque los leads de los microsites son para Walker Insurance de todas formas. El tageo por sitio (para poder reportar leads por separado) lo hace Vic desde el formulario de cada microsite, no requiere nada de Kevin. **Avance 2026-09-08 (2):** Vic confirmó que ya tiene acceso de trabajo a esa sub-account, ya ha creado formularios ahí antes sin problema. **No se cierra todavía:** falta que Vic defina el esquema de tags/campos por sitio y saque las credenciales de API y el `formId` real de cada sitio |
| W-025 | Analytics (GA4 + GTM) y captura de leads al CRM, con sitio de origen en cada lead | Vic | **Desbloqueado el 26-ago: GoHighLevel con una sub-account propia de WICFL.** Falta solo el alta de la sub-account y sus credenciales de API. **Nuevo bloqueador puntual, 2026-09-09/10:** para construir el formulario real de `/contact/` (lo que le falta a W-030 para Site #1) hace falta que Kevin diga qué campos capturar del lead — ¿nombre/teléfono/email alcanza, o también dirección de la propiedad, estatus de seguro actual, etc.? Vic ya se lo preguntó a Kevin por el chat de Zoom el 9-sep. Kevin respondió el 10-sep solo "I will have this to you soon", sin contenido todavía. El formulario real de `/contact/` sigue sin poder construirse hasta que conteste **Avance 2026-09-18:** Kevin respondió, con creces — no solo los campos pedidos (código postal, razón de compra, dirección de la propiedad, nombre/teléfono/email, timing, aseguradora actual, prima anual opcional: los 9-10 puntos que se habían preguntado), sino un diseño completo del funnel: formulario progresivo de 3 pasos, guardado del lead en el CRM antes del submit final (para no perder leads que abandonan), autocompletado de dirección, y carga opcional de la declaración de póliza actual después del submit. La pregunta de campos queda cerrada. Construir el formulario real tal como lo describió es más grande de lo que este ítem asumía — ver W-118. |
| W-103 | ~~**Modo de validación de producción: rechazar placeholders antes de publicar**~~ **Cerrado el 2026-09-04.** `scripts/check-production-config.mjs` escanea recursivamente el config construido de un sitio real buscando patrones de placeholder conocidos (`PLACEHOLDER`, `PENDING_`, teléfono terminado en `0000000`) y falla si encuentra alguno; `sites/_*` está exento por nombre. Conectado en `deploy.yml` justo antes del paso de deploy a Cloudflare, no en `preview.yml` ni en builds normales, porque Pavel itera legítimamente con algunos campos todavía pendientes de cuentas reales. Probado contra el propio demo de `stuart-homeowners` (falla correctamente, como debe) y contra un config limpio de prueba (pasa). Commit `b46f972`, sin prompt/reporte formal — ver `BITACORA.md` 2026-09-04. **Pendiente para cuando exista W-014:** hoy el paso en `deploy.yml` está hardcodeado a revisar `_example`; hay que apuntarlo al sitio real una vez exista la matriz de sitios/Worker | Vic | **Abierto el 31-ago al revisar W-020.** El schema valida la *forma*, no el contenido: `"licenseNumber": "PLACEHOLDER-FL-LICENSE"` y un GA4 `G-PLACEHOLDER` pasan la validación. Un sitio puede publicarse mostrando un número de licencia falso en todas sus páginas, que no es un bug cosmético sino un incumplimiento de publicidad de seguros en Florida. El ejecutor lo mandó al checklist humano de W-029; eso contradice la regla del propio proyecto de que todo lo automatizable va a CI. Es un segundo modo de validación que rechaza patrones conocidos (`PLACEHOLDER`, `PENDING_`, `G-PLACEHOLDER`, `GTM-PLACEHOLDER`, teléfonos con final `0000000`), cuesta cerca de una hora y corre antes de cada deploy. **Requisito añadido el 31-ago:** necesita exención explícita por ruta para `sites/_*`, porque el fixture `_example` lleva esos placeholders a propósito. Sin la exención, o el fixture rompe CI o alguien afloja el control para que pase, y aflojarlo es el desenlace peligroso **Avance 2026-09-22 (prompt 016):** el gate ya no reporta licencia contra Stuart, sin cambiar sus patrones: el placeholder desapareció del config. Hoy falla, correctamente, por `contact.email`, `contact.address.street`, `analytics.ga4`, `analytics.gtm` y `crm.formId`. Ojo: el teléfono placeholder `+17725550100` **no** lo detecta el gate (no termina en `0000000`); hay que cambiarlo antes del launch aunque el gate pase. |
| W-027 | Gate de CI de diferenciación: bloquea deploy por similaridad entre sitios | Vic | **Ancla corregida el 26-ago: antes del contenido del sitio #2, mediados de octubre.** Con un solo sitio vivo no tiene contra qué comparar. La protección durante el sitio #1 es humana y va en el handoff: Pavel corre la skill `differentiation-audit` por página conforme escribe |
| W-099 | **Automatizar** lo automatizable del checklist de W-029: llevarlo a CI en vez de a criterio humano | Vic | Se parte de W-029 el 26-ago. La lista es Bloque A porque es la definición de terminado; automatizarla es Bloque B porque solo apura, no desbloquea |
| W-114 | Configurar Cloudflare Email Routing en `stuarthomeownersinsurance.com`: reenviar `info@stuarthomeownersinsurance.com` a `info@wicfl.com` | Vic | Pedido directo de Kevin hoy 2026-09-09 por el chat de Zoom, respondiendo a la opción gratis de reenvío (Cloudflare Email Routing) que se le propuso. El dominio ya está en Cloudflare (W-011/W-013), solo falta activar Email Routing y crear la regla de reenvío. Queda agendado para mañana, 2026-09-10. |
| W-115 | ~~**Bug real: cualquier página que no sea la Home se ve cortada a la mitad del ancho**~~ **Cerrado el 2026-09-10.** Diagnosticado en vivo con JavaScript contra la página real: `BaseLayout.astro` le pone a la etiqueta `<html>` una clase `page-${pageType}`, y como casi todas las páginas del sitio son `pageType: content`, su `<html>` termina con la clase `page-content`. Esa clase ya existía, sin relación, para limitar el ancho del `<article>` de contenido (`.page-content { max-width: 46rem; }`), un selector sin ámbito que por el choque de nombres también le aplicaba ese límite de 736px a la página **entera**. Arreglo de una línea: `.page-content { max-width: 46rem; }` → `main .page-content { max-width: 46rem; }`. Ambos bridges (device_bash y la extensión de Chrome) cayeron a mitad del diagnóstico; el fix se aplicó pegando un bloque de comandos git en Codex (regla nueva, ver `feedback_copy_equipo.md`/memoria de proyecto: cuando cowork no tiene bridge para pushear, le da a Vic un copy-paste para Codex en vez de esperar). Commits: `517cbf2` (doc de W-115/W-116 a `main`), `277d7e6` (fix en `main`), `cc703d2` (mismo fix llevado a la rama del PR #8). Verificado en vivo contra `https://wicfl-pr8-stuart-homeowners.wicfl-microsites.workers.dev/high-value-home-insurance/`: el `<article>` mantiene su ancho de lectura de 46rem, el resto de la página ya ocupa el ancho completo. Liga confirmada segura para compartir con el equipo. |
| W-116 | ~~**Falta un menú de navegación real, el header solo tiene logo y teléfono**~~ **Cerrado el 2026-09-21.** El template ahora genera un nav plano sin JavaScript desde la content collection, con `navLabel` opcional y fallback a `title`, rutas reales, estado activo y estilo responsive. Las dos páginas demo se mantienen fuera del nav mediante `showInNav: false`; las páginas nuevas aparecen por defecto. Ver prompt `prompts/2026-09-10_012_menu-navegacion.md`, reporte `reports/2026-09-10_012_menu-navegacion.md`, commit `82c35ff` y CI verde [run 35603809180](https://github.com/microsites-wicfl/wicfl-microsites/actions/runs/35603809180). | Vic | **Abierto el 2026-09-10,** pregunta directa de Vic al ver que ninguna página enlaza a las demás salvo por links sueltos dentro del propio texto. Confirmado en `BaseLayout.astro`: el `<header>` solo renderiza el wordmark/logo (enlazado a `/`), la ciudad y el teléfono, cero lista de links a las páginas de cobertura. No es una decisión documentada, es un hueco real que nadie construyó. No bloquea el 9-oct de forma dura (el sitio funciona y cada página sí es alcanzable por URL o por los links internos del contenido), pero para un sitio real de cara al público es una ausencia notoria de UX/SEO. **Diseño decidido el 2026-09-10 (propuesto por cowork, sin objeción de Vic):** nav plano sin dropdown y sin JavaScript, generado desde la content collection (nunca hardcodeado por sitio), con un campo `navLabel` opcional en el frontmatter para no usar el `title` largo de SEO como texto de link. **Prompt listo:** `prompts/2026-09-10_012_menu-navegacion.md`. |
| W-117 | ~~**El formulario de contenido también crea sitios nuevos, no solo edita**~~ **Cerrado el 2026-09-21.** El formulario crea en un PR `site.config.json` e `index.md` válidos, con datos de Pavel, placeholders bloqueables para dependencias ajenas y sin facultad de merge. Prueba real: PR #11 creó el sitio, CI y preview pasaron, el gate de producción falló por los placeholders y el PR/branch se cerraron. Ver prompt `prompts/2026-09-18_013_crear-sitio-nuevo.md`, reporte `reports/2026-09-18_013_sitio-nuevo-desde-formulario.md`, commit `f503692` y CI verde [run 35606787036](https://github.com/microsites-wicfl/wicfl-microsites/actions/runs/35606787036). | Vic | **Abierto el 2026-09-18,** a partir de una pregunta directa de Vic: con Kevin confirmando Port St. Lucie como el Sitio #2, ¿cómo hace Pavel para crear ese sitio si el formulario y la guía de GitHub solo cubren editar/agregar páginas dentro de un sitio que ya existe? Confirmado que es un hueco real: ni `apps/content-form` ni `docs/OPERATOR_GUIDE.md`/`.es.md` cubren crear la carpeta `sites/<slug>/site.config.json` + `content/index.md` desde cero. **Diseño propuesto por cowork el 2026-09-18, sin objeción de Vic:** el formulario gana un modo "Crear sitio nuevo" que arma un `site.config.json` completo con lo que Pavel sabe (nicho, geografía, SEO, diferenciación) y placeholders detectables por `scripts/check-production-config.mjs` en lo que no sabe (dominio, licencia, contacto, GA4, CRM) — mismo patrón de seguridad que ya tiene la edición: nunca mergea, todo pasa por PR. Prompt listo: `prompts/2026-09-18_013_crear-sitio-nuevo.md`. |
| W-118 | **Construir el formulario real de `/contact/` según el diseño de Kevin** (3 pasos, guardado progresivo del lead, autocompletado de dirección, carga de declaración) | Vic | **Abierto el 2026-09-18,** a partir de la respuesta completa de Kevin a W-025 (ver `BITACORA.md` 2026-09-18). Sin prompt todavía — faltan tres decisiones antes de escribirlo: (1) si la integración de GoHighLevel (W-101) soporta crear/actualizar un lead a medias, o si el guardado progresivo necesita otro mecanismo para la V1; (2) qué proveedor de autocompletado de dirección se usa (dependencia externa nueva, costo/límites a revisar); (3) dónde y cómo se almacena la carga opcional de la declaración de póliza (dato sensible, necesita diseño de almacenamiento/seguridad antes de construirse). No bloquea el 9-oct de forma dura — el sitio puede lanzar con el formulario mínimo actual mientras esto se decide, pero es el reemplazo real que Kevin ya pidió. **Avance 2026-09-21:** cowork resolvió las tres decisiones de diseño (investigación real, no solo opinión): guardado progresivo vía GoHighLevel `POST /contacts/upsert` (API v3, acepta datos parciales), autocompletado de dirección vía Google Places API (gratis al volumen actual, tier de 5,000 sesiones/mes), y carga de la declaración de póliza vía Cloudflare R2 con bucket privado y URLs firmadas de corta duración. Vic las aprobó las tres. Prompt escrito: `prompts/2026-09-21_014_formulario-contacto-real.md`. **Avance 2026-09-21 (2), ejecutado por Codex:** el formulario y `apps/lead-api/` están implementados y el preview de Stuart lo muestra. GHL usa upsert inicial y update por `contactId`; Google y R2 están detrás del Worker, con pruebas mockeadas y URL R2 PUT firmada. Reporte: `reports/2026-09-21_014_formulario-contacto-real.md`. **Sigue abierto:** Vic debe provisionar GHL (token, location, formId y custom fields), Google Places y R2 (bucket, credenciales y CORS), configurar los secretos/desplegar el Worker y aprobar una prueba de punta a punta. |
| W-119 | **Ensayo del deploy a producción en un subdominio, y `deploy.yml` activado solo por dispatch** | Vic / Codex | **Abierto el 2026-09-21** al revisar next steps como nuevo arquitecto: nadie ha desplegado jamás a un dominio real; `deploy.yml` sigue en `if: false` y `wrangler.pod-1.toml` tiene apex y `www` con `custom_domain = true`, así que el primer deploy real pone el sitio en vivo en el mismo acto, sin estado intermedio. Además el apex todavía tiene dos registros `A` al parking de GoDaddy que harían fallar `custom_domain` por conflicto. Si algo de eso falla, hoy lo descubriríamos el 9-oct con Kevin mirando. **Diseño (cowork, aprobado por Vic):** Worker separado `wicfl-pod-1-rehearsal` con `wrangler.pod-1.rehearsal.toml` y una sola ruta `preview.stuarthomeownersinsurance.com`; alias de host en `pods/pod-1.json` para que el router lo resuelva; header `X-Robots-Tag: noindex` en modo ensayo; `deploy.yml` por `workflow_dispatch` con `target: rehearsal|production` y `confirm: deploy`, gate de W-103 bloqueante en production y solo informativo en rehearsal (porque los placeholders siguen puestos a propósito); y `docs/LAUNCH_RUNBOOK.md` con la secuencia del 9-oct (SSL Full strict → borrar `A`/`_domainconnect` → deploy → verificación → rollback). Prompt listo: `prompts/2026-09-21_015_ensayo-deploy-produccion.md`. Se corre la semana del 22-sep; lo que falle se arregla con una semana de margen. **Avance 2026-09-22:** código y runbook entregados en `e7b5c84`/`f777676`; el ensayo subió Worker/assets pero no pudo crear la ruta porque el token no tiene acceso a `zones/.../workers/routes`. `preview.` no resolvió; apex/www no cambiaron. Antes de cerrar: permiso mínimo de rutas/custom domains, rerun verde, y fix separado de los patterns `/*` de producción que Wrangler 4 rechaza. Reporte: `reports/2026-09-21_015_ensayo-deploy-produccion.md`. **Revisión de cowork 2026-09-22: aprobado con hallazgos.** El ensayo encontró dos bloqueadores del launch: patterns `/*` inválidos en custom domains y token sin permiso de rutas. Cowork corrigió `wrangler.pod-1.toml` (hostnames sin `/*`, mismo formato que ya pasó la validación de Wrangler en el run 35764722059) y reordenó las precondiciones del runbook. **Falta para cerrar:** Vic agrega **Zone → Workers Routes → Edit** en la zona de Stuart al token del secret `CLOUDFLARE_API_TOKEN`, rerun `rehearsal` verde, y los `curl` de `preview.`/apex/`www` en el reporte. |
| W-120 | **WICFL Studio: la plataforma de Pavel** (tablero de sitios, un borrador por sitio, editor con vista previa, publicar), reemplaza a `apps/content-form` | Vic / Codex | **Abierto el 2026-09-22** a pedido de Vic: el formulario actual expone git disfrazado (un PR por página, "review it before merging", sin preview, sin publicar, sin páginas nuevas) y es su mayor bloqueo para capacitar a Pavel. **Diseño (cowork, aprobado por Vic):** una sola app, vocabulario Borrador → Vista previa → Publicar → En vivo, nunca términos de git; **un borrador por sitio** (`draft/<slug>`, un PR, un preview del sitio completo); el repo es la base de datos, sin DB; Cloudflare Access en vez de contraseña; interfaz en español; escritura limitada a `sites/<slug>/` por construcción, lo que protege Gate A. **Fases:** A1 estructura/tablero/borrador (prompt `prompts/2026-09-22_017_studio-a1-estructura.md`), A2 editor con preview + página nueva + imágenes, A3 listo-para-publicar + Publicar + alta de sitio; meta: Pavel hace el Sitio #2 desde el 12-oct sin GitHub. Si se aprieta el calendario del launch, se corre A3 y Vic publica a mano mientras tanto. Fase B (Fase 7): provisioning, métricas, differentiation-audit en el editor. Después de A3 se retira `apps/content-form` y se reescribe la guía del operador. **Revisión 2026-09-22 del prompt 017 (commit `e24994a`): se devuelve.** Esqueleto de 63 líneas minificadas, sin reporte, sin tests de comportamiento; la app lee siempre de `main`, así que una página guardada y reabierta muestra la versión vieja; vista previa inexistente. Rehacer con `prompts/2026-09-22_018_studio-a1-completar.md`. Detalle en `reports/2026-09-22_017_studio-a1-estructura.md`. **2026-09-22, primer intento del 018 (`f6457e4`):** parcial, ~50 s. Lee del borrador y marca "Editada", pero rompió la vista de sitio (el backend cambió la forma de `pages` y la interfaz no), y el mensaje de commit declara vista previa y tests que no existen. Se continúa el mismo 018; el estado quedó escrito al inicio del prompt. **2026-09-22, continuación del 018:** 19 s, no cambió nada: su parche no aplicó porque `public/app.js` es una sola línea de 2,251 caracteres y un diff por contexto no tiene dónde agarrarse. El código minificado ya está rompiendo el trabajo del propio ejecutor. **El 018 se reemplaza por tres prompts chicos:** 019 (reescribir legible + arreglar la vista de sitio + primeros tests), 020 (vista previa real), 021 (tests restantes, menores, reporte, despliegue). **2026-09-22, A1 escrita por cowork** (excepción autorizada por Vic, registrada en `CLAUDE.md`): `apps/studio/` completo, 25 pruebas verdes en el equipo de Vic con mutation testing, prueba en navegador real sin errores. Reporte `reports/2026-09-22_019_studio-legible.md`. **Falta para cerrar A1:** revisión adversarial del ejecutor (`prompts/2026-09-22_020_studio-revision-adversarial.md`), despliegue por Vic (README, pasos 1–4) y prueba real contra Example. **2026-09-22, prompt 020:** el ejecutor agregó el job de CI de Studio (`555c7f4`, bien) pero no hizo la revisión adversarial. La hizo cowork: un problema real (la vista previa marcaba "falló" por checks ajenos al sitio, incluido el job de CI nuevo), corregido con dos pruebas rojo→verde; 27 pruebas. Reporte `reports/2026-09-22_020_studio-revision-adversarial.md`. **Falta:** despliegue por Vic y prueba real. |

---

## Fase 3 y 4 — Sitios piloto · Pavel

| id | Item | Notas |
|---|---|---|
| W-030 | Sitio #1 en inglés, generado desde config, launch 9 oct | Arranca el reloj de 120 días de Gate B. **Fecha confirmada el 25-ago** **Avance 2026-09-09:** las 8 páginas de contenido real de Pavel (Home recortada, flood, waterfront, coastal, high-value, difficult-to-insure, after-nonrenewal, contact) ya están mergeadas a `main`, PR #6, checks verdes, verificado en preview antes de mergear. Ver `BITACORA.md` 2026-09-09. **No se cierra todavía:** falta el logo/license/contacto reales de Kevin (W-008), el formulario real de `/contact/` (W-025), y que Pavel repita el proceso él mismo. |
| W-031 | Sitio #2 en español, launch 30 oct | **Pavel escribe el español nativo.** Nunca traducido ni generado desde brief en inglés **Avance 2026-09-22:** el Sitio #2 es Port St. Lucie con dominio en inglés (`portsaintluciehomeinsurance.com`), así que en la práctica **no** es el sitio en español. El sitio en español queda sin fecha ni nicho; hay que confirmarlo con Kevin y retitular este ítem. |
| W-032 | Registrar horas de ambos como línea base de Gate A | Separar desde ya **tiempo de fábrica** de **tiempo de contenido** y de **esperas de vendor**. Si la línea base mezcla los tres, no es comparable contra Gate A **Avance 2026-09-21:** no ha empezado y Pavel arrancó trabajo real hoy; cada día sin registrar es línea base perdida. Acordado con Vic: Pavel abre la hoja de horas esta semana con tres columnas (fábrica / contenido / espera de proveedor) y registra desde el 21-sep, retroactivo a hoy. |

---

## Fase 5 a 10

| id | Item | Notas |
|---|---|---|
| W-040 | Retro: catalogar todo lo que fue manual en sitios #1 y #2, y regresarlo al framework | 2–6 nov |
| W-097 | **Ensayo de Gate A**: correr el reloj completo de la fábrica contra un config de juguete en un subdominio, sin comprar dominio ni escribir contenido | 2–6 nov, dentro de Fase 5. Hallar un hueco en el runbook el 4 de noviembre cuesta una tarde; hallarlo el 10 cuesta el gate |
| W-041 | **Gate A**: cuatro criterios, todos obligatorios. Ver `docs/SCHEDULE.md`. Los dos que mandan son verificables por comando: el diff de Pavel solo toca `sites/<slug>/**`, y regenerar desde config reproduce el sitio publicado | 13 nov |
| W-050 | Automatizar registro de dominio vía Cloudflare Registrar API | API en beta: solo registra, no renueva ni transfiere |
| W-051 | Automatizar zona, DNS y attach de custom domain | |
| W-052 | Automatizar alta en Search Console y envío de sitemap | |
| W-053 | Automatizar provisioning del número de tracking | |
| W-054 | Comando end-to-end: config entra, sitio vivo sale | |
| W-060 | Reporte mensual: impresiones, clics, llamadas, cotizaciones y ventas por sitio | Corre en paralelo desde 12 oct |
| W-070 | **Gate B**: decisión comercial de Kevin contra el número de W-005 | **5 mar 2027** (movido: los 120 días del sitio #2 caen el 27 de feb) |

---

## Watch (sin fecha, no bloquea nada todavía)

| id | Item | Cuándo importa |
|---|---|---|
| W-104 | **Cinco vulnerabilidades transitivas en el árbol de dependencias de build**, cuatro de severidad alta | Antes de que el pipeline construya sitios públicos. Reportadas el 31-ago al instalar el workspace. Exposición acotada: son dependencias de build y la salida de Astro es estática, así que nada de eso se sirve a los visitantes. No se corrió `npm audit fix --force`, que habría metido cambios de dependencias sin revisar. Revisar una por una y decidir |
| W-090 | Confirmar con Cloudflare el alta masiva de zonas en plan free | A los ~25 dominios, no a los 90 |
| W-091 | Evaluar si conviene hub-and-spoke en vez de portfolio de dominios | Solo si Gate B falla |

---

## Done

| id | Item | Cerrado | Resultado |
|---|---|---|---|
| W-002 | Dueño del contenido y escritor de español | 2026-08-25 | Kevin asignó el rol de contenido a Pavel, **y Pavel escribe español nativo.** Elimina la contratación que creíamos necesaria antes de finales de octubre. Se revisa en Gate B por el techo de escala |
| W-009 | Disponibilidad de Pavel | 2026-08-25 | Confirmada con dedicación al proyecto. **La Fase 3 queda fija el 21 de septiembre.** (Gate B se movió después al 5 de marzo por los 120 días del sitio #2; ver W-070) |
| W-092 | Correo de empresa que gestiona el proyecto | 2026-09-01 | **`microsites@wicfl.com`**, creado por Kevin. Desbloquea W-010, W-012, W-101 y toda la Fase 1. La credencial llegó por el chat de Zoom, así que **se rota en el primer login y la nueva vive solo en el vault**; ver W-010 |
| W-096 | Qué CRM usa WICFL | 2026-08-26 | **GoHighLevel con sub-account propia**, no la instancia de WAGS. Desbloquea W-025. Queda como tarea de Vic dar de alta la sub-account y sacar credenciales, no como pregunta a Kevin |
| W-001 | Quote escrito de GoTo por número mensual | 2026-08-25 | **$0.99 por número.** $1,188/año a 100 números, por debajo de la ruta de forwarding. Se compran directo en GoTo, se descarta Twilio. Ver `docs/COST_MODEL.md` |
| W-095 | Quién revisa el contenido de Pavel antes de publicar | 2026-09-03 | **Decidido por Vic, no por Kevin: no hay más gente disponible.** Pavel se autorrevisa contra el checklist de cumplimiento de `docs/CONTENT_STANDARDS.md` antes de publicar cada página. Puede pedirle una segunda opinión a Kevin de manera informal cuando algo sea ambiguo, pero no es un paso oficial ni bloquea el publish. Se retira de los bloqueadores de Kevin; se revisita si el portafolio crece más allá de los tres sitios piloto, porque a veinte sitios la misma persona escribiendo, optimizando y aprobando compliance deja de ser sano |
