# Reporte: 2026-09-10_010 — SEO técnico: canonical, robots.txt, sitemap.xml, schema.org

## Qué se hizo

- Se agregó el prop `path` a `BaseLayout` y un canonical absoluto calculado con
  `site.domain` y la ruta real de cada página.
- `index.astro` pasa `/`; `[...slug].astro` pasa `routeForPageId(page.id)` para conservar las
  rutas y locales existentes.
- Se agregaron los endpoints estáticos `robots.txt.ts` y `sitemap.xml.ts`.
- Se agregó JSON-LD `InsuranceAgency` a cada página, con nombre, teléfono, email, dirección y,
  cuando existe, área de servicio del config.

## Decisiones tomadas

El sitemap usa `getCollection("pages")` y `routeForPageId` para que haya una única fuente de
verdad para las rutas, incluido el prefijo de idiomas. Omite `lastmod`: el repo no tiene una
fecha de modificación fiable por página.

El JSON-LD se serializa en el layout y reemplaza `<` antes de insertarse en el script, para que
los valores de configuración no puedan cerrar la etiqueta. Se usa `is:inline` para que Astro no
procese el bloque JSON-LD como un script de aplicación.

## Verificación

`npm run check` pasó:

```text
packages/config-schema/examples/seguro-casa-miami.example.json valid
packages/config-schema/examples/stuart-flood.example.json valid
sites/_example/site.config.json valid
sites/stuart-homeowners/site.config.json valid
Result (10 files):
- 0 errors
- 0 warnings
- 0 hints
```

`npm run build:site -- stuart-homeowners` construyó 10 páginas, más `robots.txt` y
`sitemap.xml`. Los HTML generados contienen:

```text
home:     https://stuarthomeownersinsurance.com/
interior: https://stuarthomeownersinsurance.com/high-value-home-insurance/
```

`dist/sites/stuart-homeowners/robots.txt` contiene
`Sitemap: https://stuarthomeownersinsurance.com/sitemap.xml`. Su sitemap se parseó como XML y
tiene 10 entradas, igual que los 10 archivos Markdown de `sites/stuart-homeowners/content/`.

El JSON-LD de la home se parseó correctamente: `@type` es `InsuranceAgency`, con nombre
`Stuart Homeowners Insurance (Demo)`, teléfono `+17725550100`, email
`demo-placeholder@stuarthomeownersinsurance.com` y dirección configurada en Stuart, FL 34994.

`npm run build:site -- _example` también pasó. El fixture bilingüe generó, entre otras, la ruta
`/es/about-fixture/`; su canonical y sitemap contienen
`https://example-flood.invalid/es/about-fixture/`.

La validación remota de GitHub Actions queda pendiente del push de este commit. Se añadirá aquí
la URL y el estado comprobado de la corrida antes del cierre final.

## Lo que tocaste fuera de lo pedido

Nada. Solo se modificaron el layout y las páginas requeridas para pasar la ruta, se añadieron los
dos endpoints pedidos y se actualizaron los archivos de reporte y seguimiento obligatorios.

## Lo que no pudiste verificar

La ejecución de GitHub Actions para este cambio aún no existía durante la verificación local. Se
verificará directamente después del push, antes del cierre final.

## Dónde dudaste

El número de licencia de seguros existe en el config, pero no se incluyó: el prompt prohíbe
inventar una propiedad schema.org para él y no identifica una propiedad estándar adecuada.

## Qué te sorprendió del repo

Los builds de Stuart y del fixture emitieron el aviso existente de Astro sobre un `index` de
content collection duplicado, aunque ambos terminaron correctamente. También se mantuvo el
aviso existente de Tailwind sobre `content` vacío. Ninguno produjo un error ni requirió un cambio
para W-023.

## Lo que no se hizo

No se agregaron dependencias, no se modificó `scripts/check-production-config.mjs`, no se
alteró el selector protegido de W-115, ni se validaron dominios fixture como dominios de
producción. Son controles fuera del alcance de este prompt.

## Próximos pasos sugeridos

Cowork debe revisar el diff y la corrida de CI remota. La licencia y los datos de contacto de
producción siguen siendo dependencias de W-008; el JSON-LD tomará automáticamente sus valores
reales cuando el config se actualice.

## Commits

- Pendiente de crear y verificar en GitHub Actions.
